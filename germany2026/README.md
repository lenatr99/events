# Germany 2026

Project layout:

- `assets/images/` stores event assets grouped by subject.
- `docs/` stores supporting project documents, such as the packing list.
- `itinerary/` stores the generated itinerary, config, and per-day source files.
- `references/` stores raw reference material that is not part of the generated itinerary.
